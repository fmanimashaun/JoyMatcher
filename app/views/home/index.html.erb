<%# Temporary stub — will be replaced with full landing page in Phase 3 %>
<div class="min-h-screen flex items-center justify-center">
  <div class="card max-w-md mx-auto text-center">
    <h1 class="text-2xl font-semibold text-[hsla(320,50%,15%,1)] mb-4">
      Welcome to JoyMatcher
    </h1>
    <p class="text-[hsla(320,30%,35%,1)] mb-6">
      A trust-based relationship platform.
    </p>
    <div class="flex gap-3 justify-center">
      <%= link_to 'Sign In', new_session_path, class: 'btn btn-primary' %>
      <%= link_to 'Create Account', new_registration_path, class: 'btn btn-outline' %>
    </div>
  </div>
</div>
