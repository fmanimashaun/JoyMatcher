<%# Stub — full dashboard built in Phase 3 %>
<div class="max-w-4xl mx-auto py-8 px-4">
  <div class="card">
    <h1 class="text-2xl font-semibold text-[hsla(320,50%,15%,1)] mb-2">
      Your Dashboard
    </h1>
    <p class="text-[hsla(320,30%,35%,1)]">
      Welcome, <%= current_user.first_name.presence || current_user.email_address %>
    </p>
  </div>
</div>
