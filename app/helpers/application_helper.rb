module ApplicationHelper
  # Pagy v43: helpers are instance methods on @pagy object
  # Usage in views: @pagy.series_nav, @pagy.page_url(page), etc.
  # No Pagy::Frontend module — built into pagy instances directly
end
